# low = 0
# high = len(list) - 1
# mid = (low + high) / 2 
# guess = list[mid]

def binary_search(list, item):
	low = o
	high = len(list)-1

	while low <= high:
		mid = (low + high)
		guess = list[mid]